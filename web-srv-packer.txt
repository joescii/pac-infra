{
  "builders": [
    {
      "type": "amazon-ebs",
      "region": "us-east-1",
      "source_ami": "ami-de0d9eb7",
      "instance_type": "t1.micro",
      "ssh_username": "ubuntu",
      "ami_name": "packer-quick-start {{timestamp}}"
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "execute_command": "echo 'java' | {{ .Vars }} sudo -E -S sh '{{ .Path }}'",
      "inline": [
        "sleep 10",
        "/usr/bin/apt-get install python-software-properties",
        "add-apt-repository ppa:webupd8team/java",
        "/usr/bin/apt-get update",
        "/usr/bin/apt-get install oracle-java8-installer",
        "sleep 5",
        "java -version"
      ]
    },
    {
      "type": "shell",
      "execute_command": "echo 'git' | {{ .Vars }} sudo -E -S sh '{{ .Path }}'",
      "inline": [
        "sleep 10",
        "/usr/bin/apt-get -y install git"
      ]
    },
    {
      "type": "shell",
      "execute_command": "echo 'git' | {{ .Vars }} sudo -E -S sh '{{ .Path }}'",
      "inline": [
        "sleep 5",
        "export JAVA_HOME=/usr/lib/jvm/java-8-oracle/",
        "git clone https://github.com/joescii/prose-and-conz.git",
        "cd prose-and-conz",
        "sbt package"
      ]
    }
  ]
}